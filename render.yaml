services:
  - type: web
    name: HoJin-bucket-BE
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "alembic revision --autogenerate && alembic upgrade head && uvicorn main:app --host 0.0.0.0 --port $PORT"
    plan: free
